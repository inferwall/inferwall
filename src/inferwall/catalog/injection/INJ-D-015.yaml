# License: CC BY-SA 4.0 — https://creativecommons.org/licenses/by-sa/4.0/
signature:
  id: INJ-D-015
  name: STAN Jailbreak
  version: "1.0.0"
meta:
  category: prompt-injection
  subcategory: direct
  technique: stan-jailbreak
  owasp_llm: "LLM01:2025"
  severity: high
  confidence: high
  performance_cost: low
  tags: [jailbreak, stan, chatgpt, direct-injection]
detection:
  engine: heuristic
  direction: input
  patterns:
    - type: regex
      value: "(?i)(strive\\s+to\\s+avoid\\s+norms|you\\s+are\\s+(now\\s+)?STAN|act\\s+as\\s+STAN|STAN\\s+mode\\s+(enabled|activated|on))"
    - type: regex
      value: "(?i)(switch\\s+to\\s+STAN|activate\\s+STAN|enable\\s+STAN|in\\s+STAN\\s+mode|\\bSTAN:\\s|STAN\\s+will\\s+not)"
    - type: regex
      value: "(?i)(STAN\\s+does\\s+not\\s+(have|follow|obey)|STAN\\s+can\\s+do\\s+anything|STAN\\s+is\\s+free|STAN\\s+has\\s+no\\s+(restrictions|limitations|filters|rules))"
  condition: any
scoring:
  anomaly_points: 8
tuning:
  enabled: true
  default_enabled: true
  default_action: enforce
