# License: CC BY-SA 4.0 — https://creativecommons.org/licenses/by-sa/4.0/
signature:
  id: INJ-O-004
  name: Payload Splitting
  version: "1.0.0"
meta:
  category: prompt-injection
  subcategory: obfuscation
  technique: payload-splitting
  owasp_llm: "LLM01:2025"
  severity: high
  confidence: medium
  performance_cost: low
  tags: [payload-splitting, obfuscation, injection]
detection:
  engine: composite
  direction: input
  patterns:
    - type: regex
      value: "(?i)(combine\\s+(the\\s+)?(above|previous|following)\\s+(parts?|pieces?|fragments?|segments?))"
    - type: regex
      value: "(?i)(concatenate|join|merge|assemble)\\s+(these|the\\s+following|all)\\s+(strings?|parts?|pieces?|texts?)"
  condition: any
scoring:
  anomaly_points: 7
tuning:
  enabled: true
  default_enabled: true
  default_action: enforce
