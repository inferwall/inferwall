# License: CC BY-SA 4.0 — https://creativecommons.org/licenses/by-sa/4.0/
signature:
  id: INJ-O-016
  name: Spanish/French/Multilingual Injection
  version: "1.0.0"
meta:
  category: prompt-injection
  subcategory: obfuscation
  technique: multilingual-injection
  owasp_llm: "LLM01:2025"
  atlas: ["AML.T0068", "AML.T0051.000"]
  severity: high
  confidence: high
  performance_cost: low
  tags: [injection, obfuscation, multilingual, spanish, french, italian, russian]
detection:
  engine: heuristic
  direction: input
  patterns:
    - type: regex
      value: "(?i)(olvid[ae]\\s+todo|ignora\\s+todas?\\s+(las\\s+)?(instrucciones|reglas)|no\\s+sigas\\s+(las\\s+)?instrucciones|solo\\s+di(ces|gas|me))"
    - type: regex
      value: "(?i)(oubliez?\\s+tout|ignorez?\\s+(toutes?\\s+)?(les\\s+)?(instructions|règles|consignes)|ne\\s+suivez?\\s+pas)"
    - type: regex
      value: "(?i)(dimentica\\s+tutto|ignora\\s+tutte\\s+le|non\\s+seguire\\s+le\\s+istruzioni)"
    - type: regex
      value: "(?i)(игнорируй|забудь\\s+все|забудь\\s+предыдущие)"
  condition: any
scoring:
  anomaly_points: 7
tuning:
  enabled: true
  default_enabled: true
  default_action: enforce
