# License: CC BY-SA 4.0 — https://creativecommons.org/licenses/by-sa/4.0/
signature:
  id: INJ-D-007
  name: Translation Bypass
  version: "1.0.0"
meta:
  category: prompt-injection
  subcategory: direct
  technique: translation-bypass
  owasp_llm: "LLM01:2025"
  atlas: ["AML.T0068", "AML.T0051.000"]
  severity: medium
  confidence: medium
  performance_cost: low
  tags: [translation, bypass, direct-injection]
detection:
  engine: classifier
  direction: input
  patterns:
    - type: regex
      value: "(?i)(translate\\s+(the\\s+following|this)\\s+(to|into).{0,30}(ignore|bypass|override|disregard))"
    - type: regex
      value: "(?i)(respond\\s+in\\s+(another|a\\s+different)\\s+language.{0,30}(rules|restrictions|guidelines)\\s+(don'?t|do\\s+not)\\s+apply)"
  condition: any
scoring:
  anomaly_points: 6
tuning:
  enabled: true
  default_enabled: true
  default_action: enforce
